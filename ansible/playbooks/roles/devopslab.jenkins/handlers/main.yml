---
  - name: Reload systemd
    ansible.builtin.systemd:
      daemon_reload: true

  - name: Restart Jenkins
    ansible.builtin.systemd:
      name: jenkins
      state: restarted

  - name: Restart Jenkins after systemd reload
    ansible.builtin.systemd:
      name: jenkins
      state: restarted
      daemon_reload: true